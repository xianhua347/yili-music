name: Deploy to Server
env:
  CONTAINER_NAME: "yili-music"
on:
  push:
    branches: [ "master" ]
  pull_request:
    branches: [ "master" ]

jobs:
  deploy:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout code
        uses: actions/checkout@v2

      - name: Login to Docker Hub
        uses: docker/login-action@v1
        with:
          username: ${{ secrets.DOCKER_USERNAME }}
          password: ${{ secrets.DOCKER_PASSWORD }}

      - name: Build and Push Docker Image
        uses: docker/build-push-action@v2
        with:
          context: .
          push: true
          tags: chenxianhua123/${{ env.CONTAINER_NAME }}:latest

      - name: Push Docker Image to Docker Hub
        run: chenxianhua123/${{ env.CONTAINER_NAME }}:latest

      - name: SSH into remote server
        uses: appleboy/ssh-action@master
        with:
          host: 47.120.3.222
          username: ${{ secrets.REMOTE_SERVER_USERNAME }}
          password: ${{ secrets.REMOTE_SERVER_PASSWORD }}
          script: |
            docker pull chenxianhua123/${{ env.CONTAINER_NAME }}:latest
            docker stop ${{ env.CONTAINER_NAME }} || true
            docker rm ${{ env.CONTAINER_NAME }} || true
            docker run -d --name ${{ env.CONTAINER_NAME }} -p 5431:8080 chenxianhua123/${{ env.CONTAINER_NAME }}
